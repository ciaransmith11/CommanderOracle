{
  "name": "commander-oracle",
  "version": "0.0.0",
  "private": true,
  "description": "MTG Commander (EDH) deck-building advisor",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter @commander-oracle/web build",
    "start": "pnpm --filter @commander-oracle/server start",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
